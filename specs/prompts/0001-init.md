You are a senior Rust infrastructure architect, tooling designer, and architecture governance engineer.

You are working on the the ecosystem, which has a dedicated architecture repository named `architecture repo` and an existing CLI/tooling concept named `holoarch`.

Your task is to design and implement the next-generation architecture governance system for the ecosystem.

This is not a brainstorming task. This is a concrete architecture + implementation planning task.

You must produce a detailed, implementation-oriented blueprint for turning `architecture repo` into the source of truth for ecosystem architecture governance, and `holoarch` into the CLI/tooling layer that applies, validates, syncs, and reports those standards across all downstream repositories.

Do not give shallow advice. Do not stay at a vague strategic level. Produce a system that could actually be built.

---

# Context

We have a multi-repo ecosystem around the project.

We want `architecture repo` to define and preserve:

- high-level system architecture
- design decisions
- standards all repos should follow
- architectural boundaries
- crate relationships and dependency direction rules
- contracts between repos
- process for architecture changes
- ADRs
- exceptions
- repository conformance rules

We want a tooling system that can manage and enforce this consistently.

We already have or plan to have a CLI/tool called `holoarch`.

We want `holoarch` to become the architecture governance operator for the ecosystem.

This system should help us:

- initialize new repositories from architecture standards
- sync managed templates and standard sections
- verify conformance
- create ADRs and exception records
- detect standards drift
- inspect repo metadata
- report contract/dependency relationships
- support CI enforcement
- scale across many repos cleanly

---

# Required Output

Produce a single cohesive response that includes ALL of the following:

## 1. Architecture model
Define the full architecture governance model for:

- `architecture repo`
- `holoarch`
- downstream repos

Clearly explain the separation of responsibility and source-of-truth boundaries.

You must define how architecture content, policies, templates, schemas, metadata, exceptions, and conformance checks fit together.

---

## 2. Repository structure for `architecture repo`
Design the repository structure for `architecture repo`.

It must include:
- human-readable docs
- ADRs
- templates
- policy files
- schema files
- examples
- process documents

Provide a proposed directory tree.

Also explain what belongs in each directory and what does not.

---

## 3. Governance categories
Define the categories of architecture information and how they should be handled.

At minimum include:
- vision / principles
- architecture
- standards
- contracts
- ADRs
- process
- exceptions
- templates
- machine-readable policy

Explain how each category differs and how they interact.

---

## 4. Downstream repository metadata model
Define the metadata format each downstream repo should include.

Provide a concrete example file such as `archon.repo.yaml`.

It should cover things like:
- repo name
- role
- repo type
- standards version
- architecture version
- owners
- contracts implemented
- contracts depended on
- crate classes
- exceptions

Be explicit and practical.

---

## 5. `holoarch` tool design
Design `holoarch` as a Rust workspace.

Provide:
- workspace directory layout
- crate-by-crate breakdown
- responsibilities for each crate
- major internal modules if useful

You must include CLI, core types, repo inspection, templates, policy, verification, ADR support, graph/reporting, and sync/diff support.

---

## 6. Command design
Define the `holoarch` CLI surface.

You must include at least:
- `init`
- `sync`
- `verify`
- `adr new`
- `exception new`
- `status`
- `graph`

For each command, define:
- purpose
- expected behavior
- useful flags
- what files it reads
- what files it writes
- safety expectations

---

## 7. File ownership and sync model
Define a safe model for file management.

You must distinguish between:
- fully managed files
- section-managed files
- unmanaged files

Explain how `holoarch sync` should work safely without clobbering handwritten content.

Include a concrete example of a `sync-manifest.yaml` or equivalent.

---

## 8. Conformance model
Define a formal conformance model with levels.

At minimum cover:
- structural conformance
- policy conformance
- architectural conformance

Then define the actual checks that should run, such as:
- required files
- required directories
- metadata validity
- standards version presence
- exception references
- docs minimums
- crate taxonomy
- dependency direction rules
- ADR validity

Be concrete.

---

## 9. Policy model
Define the machine-readable policy system.

Explain:
- what policy files should exist
- suggested schema shape
- which rules should be configurable
- how policies are versioned
- how `holoarch` loads and evaluates them

You may use YAML/TOML/JSON, but be consistent and justify the choice.

---

## 10. Versioning strategy
Define how architecture versioning and standards versioning should work.

We want explicit version markers so that repos can declare what they conform to.

Include:
- architecture version
- standards version
- possible migration thinking
- how drift should be reported

---

## 11. ADR and exception system
Define a practical ADR workflow and an exception workflow.

Explain:
- how ADRs are created
- numbering rules
- statuses
- relationship to standards/contracts
- how repo-level exceptions should work
- whether exceptions should expire or require review
- how exceptions should be referenced from metadata

Include example structures.

---

## 12. CI and workflow integration
Define how this should be used in CI and day-to-day development.

Include:
- minimum CI usage
- strict mode behavior
- warning vs failure guidance
- how repo maintainers update after standards changes
- how architecture changes propagate from `architecture repo` into downstream repos

---

## 13. MVP and phased implementation plan
Provide a phased implementation roadmap for `holoarch`.

At minimum include:
- MVP / phase 1
- phase 2
- phase 3
- optional later phases

Each phase should be practical and sequenced well.

---

## 14. Initial deliverables
List the first concrete files/docs/crates/commands we should implement next.

This should be actionable and prioritized.

---

# Constraints

Follow these constraints carefully:

- Prefer explicit governance over implicit convention.
- Prefer machine-readable policy for anything we may want to verify in CI.
- Keep `architecture repo` as the source of truth for standards and policy content.
- Do NOT make `holoarch` the owner of architecture content; it is the executor/enforcer/operator.
- Do NOT assume a monorepo. This must support many separate repositories.
- Downstream repos must be able to declare approved exceptions rather than silently drift.
- Sync must be safe and must not blindly overwrite handwritten files.
- The system should be practical for a Rust-heavy ecosystem, especially Rust workspaces with multiple crates.
- The design should be implementation-ready, not just conceptual.
- Prefer durable architecture governance and auditability.
- Assume we want CI enforcement eventually.
- Assume we may later generate docs sites or dashboards from the same metadata/policy graph.

---

# Strong design preferences

In your answer, strongly prefer:

- a clean separation between docs, templates, policy, and schema
- a real repo metadata file in downstream repos
- date-based standards versioning such as `2026.03`
- a Rust workspace for `holoarch`
- a verify-first governance model
- explicit exception records
- a safe sync-manifest-based update model
- conformance checks that can grow over time
- architecture categories that keep the repo from becoming a pile of notes

---

# Output format requirements

Your response must be highly structured with clear headings.

Include:
- repository trees
- example metadata YAML
- example policy file(s)
- example sync manifest
- example command list
- example ADR or exception structure where useful

Do not ask me questions.
Do not defer work.
Do not give a short summary instead of the full design.
Do not say “here’s a starting point” and stop early.
Deliver the full implementation-oriented blueprint in one response.

Make the result strong enough that I can use it to drive implementation work in `architecture repo` and `holoarch` immediately.